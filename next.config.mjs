/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['cloudflare-ipfs.com', 'loremflickr.com', 'www.dropbox.com', 'www.lukecase.com','img.freepik.com']
    }
};

export default nextConfig;
